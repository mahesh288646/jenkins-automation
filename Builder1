import src.main.groovy.jenkins.automation.builders.*
import src.main.groovy.jenkins.automation.utils.EnvironmentUtils

// the "JAC_ENVIRONMENT" global variable is set in all of our Jenkinses, to either "DEV", "STAGE", or "PROD"

def env = EnvironmentUtils.getInstance("${JAC_ENVIRONMENT}")

if (env.isDev()) {
    new BaseJobBuilder(
            name: "a-sample-job",
     ).build(this)
}